apiVersion: v1
kind: Service
metadata:
  name: dashboard-api
  namespace: apps
  labels:
    app.kubernetes.io/name: dashboard-api
    app.kubernetes.io/part-of: order-fulfillment
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: dashboard-api
  ports:
    - name: http
      port: 8086
      targetPort: http

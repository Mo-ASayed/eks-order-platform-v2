apiVersion: apps/v1
kind: Deployment
metadata:
  name: shipping-service
  namespace: apps
  labels:
    app.kubernetes.io/name: shipping-service
    app.kubernetes.io/part-of: order-fulfillment
spec:
  # HPA owns replicas
  selector:
    matchLabels:
      app.kubernetes.io/name: shipping-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: shipping-service
        app.kubernetes.io/part-of: order-fulfillment
    spec:
      # Terraform eventbus trusts this service account for SQS publish access.
      serviceAccountName: shipping-service
      containers:
        - name: shipping-service
          image: 767398132018.dkr.ecr.eu-west-2.amazonaws.com/shipping-service
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: shipping-service-config
            - secretRef:
                name: shipping-service-secrets
          ports:
            - name: http
              containerPort: 8085
          livenessProbe:
            httpGet:
              path: /livez
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
            timeoutSeconds: 2
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
